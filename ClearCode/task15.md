#### 1. // Метод для вычисления факториала числа n
```
public int factorial(int n) {
    return (n == 1 || n == 0) ? 1 : factorial(n - 1) * n;
}
```
Информативный комментарий
___

#### 2. // Обратите внимание: данный алгоритм имеет временную сложность O(n^2).
```
public void sortArray(int[] array) {
    // реализация метода сортировки
    // Код...
}
```
Усиление
___

#### 3. // TODO - Добавить тесты для этого метода.
```
public String processInput(String input) {
    // реализация метода
    // код...
}
```
Комментарий TODO
___

#### 4. // Метод для получения списка всех доступных товаров из базы данных
```
public List<Product> getAllProducts() {
    // реализация метода
   // код...
}
```
Информативный комментарий
___

#### 5. // Создаем экземпляр класса для работы с API
```
ApiClient apiClient = new ApiClient();
```
Представление намерения
___

#### 6. // Возвращает среднее значение чисел в массиве
```
double average = calculateAverage(numbers);
```
Прояснение 
___

#### 7. // Обратите внимание: этот шаг является обязательным для корректной работы всей системы.
```
initializeDependencies();
```
Усиление
___

#### 8. // TODO - Оптимизировать этот метод для повышения производительности.
```
public void heavyComputation() {
    // реализация метода с высокой вычислительной нагрузкой
    // код...
}
```
Комментарий TODO
___

#### 9. // TODO - Добавить обработку исключений для этого метода.
```
public void performAction() {
    // реализация метода без обработки ошибок
    //код
}
```
Комментарий TODO
___

#### 10. // Не используйте этот метод в многопоточной среде без синхронизации.
```
public void updateSharedResource() {
    // реализация метода
    // код...
}
```
Предупреждение о последствиях 
___

#### 11. // Создаем объект для работы с базой данных
```
DatabaseConnection dbConnection = new DatabaseConnection();
```
Представление намерения
___

#### 12. // Важно: не забывайте вызывать close() для освобождения ресурсов.
```
FileInputStream fis = new FileInputStream("file.txt");
// ... код
fis.close();
```
Усиление 
___